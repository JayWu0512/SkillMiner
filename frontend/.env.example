# === App behavior (dev only) ===
# If true, skip real auth flow and use mock state in development.
VITE_DEV_BYPASS_AUTH=
# Which page to open after dev boot. Options: "upload" | "chat"
VITE_DEV_START_STAGE=

# === Frontend base URL (must match Supabase Auth URL settings) ===
# Your Vite dev server URL (change if you use a different port).
VITE_APP_BASE_URL=

# === API endpoints ===
# Your own backend (local).
VITE_API_BASE=

# === Supabase config (public) ===
# Found in: Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# === Optional: feature flags / logging ===
# INFO | DEBUG | WARN | ERROR
VITE_LOG_LEVEL=